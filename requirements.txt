# SP-API
python-amazon-sp-api>=0.19.14

# Database
# Older supabase (2.3.x) requires httpx 0.24 which has a httpcore<0.18 cap that
# conflicts with everything else on Streamlit Cloud. Modern versions (2.15+)
# drop that constraint and resolve cleanly.
supabase>=2.15.0,<3.0.0

# Scheduler
APScheduler>=3.10.4

# Dashboard
streamlit>=1.32.0
plotly>=5.20.0
openpyxl>=3.1.2

# Data
pandas>=2.2.0

# Google Sheets (Forecast)
gspread>=6.0.0
google-auth>=2.28.0

# Utilities
python-dotenv>=1.0.1
requests>=2.31.0
tenacity>=8.2.3
